/**
 * Conversion from radians to degrees.
 *
 * @param {Number} rad the angle in radians.
 * @return {Number} the angle in degrees.
 */
const radToDeg = rad => 180.0 * (rad / Math.PI);

export { radToDeg };
